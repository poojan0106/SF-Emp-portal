/**
 * SwipeEventTrigger
 * Handles talent pool membership on swipe right
 */
trigger SwipeEventTriggers on Swipe_Event__c (after insert) {
    // Legacy trigger intentionally disabled in code.
    // SwipeEventTrigger + SwipeEventHandler is the single source of truth.
}